import 'package:flutter/material.dart';

class PrimeryButton extends StatelessWidget {
final String text;
   PrimeryButton({super.key,} );

  @override
  Widget build(BuildContext context) {
    return 
  }
}